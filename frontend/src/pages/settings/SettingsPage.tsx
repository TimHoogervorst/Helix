// Re-export shim — the canonical file has moved to core-mods/lims/settings/SchemaSettings.ts.
// Prefer importing from the canonical location in new code.
export { default } from "../../core-mods/lims/settings/SchemaSettings";
